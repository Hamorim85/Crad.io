<!--


<p id="notice"><%= notice %></p>
  <div class="tabs">
    <a class="tab active" href="#">
      <h3><%= @influencers.count %></h3>
      <p>My Influencer Database</p>
    </a>
    <a class="tab" href="#">
      <h3>96</h3>
      <p>Influencers Contacted</p>
    </a>
    <a class="tab" href="#">
      <h3>36</h3>
      <p>Influencers responded</p>
    </a>
    <a class="tab" href="#">
      <h3>13</h3>
      <p>Influencers worked with</p>
    </a>
  </div>

<div class="custom-container container">
  <h1>Influencers</h1>

  <table class="table table-striped">
    <thead>
      <tr>
        <th></th>
        <th>Photo</th>
        <th>Username</th>
        <th>Email</th>
       <th>Country</th>
        <th>Follow Ratio</th>
        <th>Followers count</th>
        <th>Following count</th>
        <th>Media count</th>
        <th>Instagram ID</th>
        <th>Full name</th>
        <th colspan="4"></th>
      </tr>
    </thead>

    <tbody>
      <% @influencers.each do |influencer| %>
        <tr>
          <% if influencer.verified  %>
            <td><i class="fas fa-check-circle"></i></td>
          <% else %>
            <td></td>
          <% end %>
          <td><%= image_tag "", size:"50x50" %></td>
          <td><%= influencer.username %></td>
          <td><%= influencer.email %></td>
          <td><%= influencer.country %></td>
          <td><i class="fas fa-chevron-<%= influencer.follow_ratio > 1 ? "up green" : "down red" %>"></i></td>
          <td><%= influencer.followers_count %></td>
          <td><%= influencer.following_count %></td>
          <td><%= influencer.media_count %></td>
          <td><%= influencer.igid %></td>
          <td><%= influencer.full_name %></td>
          <td><%= link_to 'Website', influencer.external_url %></td>
          <td><%= link_to 'Show', influencer %></td>
          <td><%= link_to 'Edit', edit_influencer_path(influencer) %></td>
          <td><%= link_to 'Destroy', influencer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>
</div>

<%= link_to 'Manually add an Influencer to your database', new_influencer_path %>
 -->
